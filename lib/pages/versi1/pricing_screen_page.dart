import 'package:flutter/material.dart';

class PricingScreenV1 extends StatelessWidget {
  const PricingScreenV1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Text('Pages Pricing Screen Versi 1'),
      ),
    );
  }
}
