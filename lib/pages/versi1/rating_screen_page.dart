import 'package:flutter/material.dart';

class RatingScreenV1 extends StatelessWidget {
  const RatingScreenV1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Text('Pages Rating Screen Versi 1'),
      ),
    );
  }
}
