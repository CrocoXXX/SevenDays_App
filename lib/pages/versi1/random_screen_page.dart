import 'package:flutter/material.dart';

class RandomScreenV1 extends StatelessWidget {
  const RandomScreenV1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Text('Pages Random Screen'),
      ),
    );
  }
}
