import 'package:flutter/material.dart';

class PricingScreenV2 extends StatelessWidget {
  const PricingScreenV2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Text('Page Pricing Screen Versi 2'),
      ),
    );
  }
}
