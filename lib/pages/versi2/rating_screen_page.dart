import 'package:flutter/material.dart';

class RatingScreenV2 extends StatelessWidget {
  const RatingScreenV2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Text('Pages Rating Screen Versi 2'),
      ),
    );
  }
}
